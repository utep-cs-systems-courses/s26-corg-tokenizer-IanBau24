# obj list
OBJS = uimain.o # for now i just listed uimain.o as a file that should exist 
# compiler flags.  -g for debug, -O3 for optimization
CFLAGS=-g -O3

# first target is built by default
all: token # so far i understand this is the name of my build

# ? will add later if necessary
# # include file dependencies
# bst.o: bst.h
# sortargs.o: bst.h

#? made my build depend on the obj list
# $@=target, $^=prerequisites
token: $(OBJS) 
	cc -o token $(CFLAGS) $(OBJS)

#? when making clean it will basically remove all .o file forcefully
# deletes files generated by compilation
clean:
	rm -f *.o token

# ? run command
run:token
	./token

